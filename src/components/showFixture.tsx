import { Container, Typography } from "@mui/material";

export default function ShowFixture() {
  return (
    <Container maxWidth="lg">
      <Typography variant="h4" sx={{ mt: 5 }}>Show Fixture (Coming Soon)</Typography>
    </Container>
  );
}
